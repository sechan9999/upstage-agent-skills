# 결선 서비스 — Team AI Cost Radar (팀 AI 비용 레이더)

> **부문:** 결선 — "모두를 위한 서비스"
> **한 줄:** 예선의 개인용 **Token Cost Optimizer**를, 팀 전체의 AI 비용 낭비를 진단·시각화·절감하는 **팀 대시보드 서비스**로 확장.
> **환경:** Solar Pro 4 (분석·추천 엔진) × 타임리 Agent (오케스트레이션)

> *"12개월 뒤, 월 \$200 쓰는 개발자와 \$4,000 쓰는 개발자의 격차는 실력이 아니라 라우팅이다."*
> 개인이 아니라 **팀 전체**의 라우팅을 표준화하면, 절감은 배가된다.

---

## 1. 문제 (팀 규모)
개인은 자기 낭비를 잘 못 봅니다. 팀은 더 못 봅니다 — 누가 어떤 작업에 어떤 모델을 쓰는지 아무도 전체를 모르고, 비용은 청구서로만 뒤늦게 드러납니다. 결과: 팀마다 같은 낭비(과잉 로딩, 최상위 모델 남용, 캐시 깨짐)를 각자 반복.

## 2. 누구를 위한 (Who)
- **엔지니어링 팀 리드** — 팀 AI 비용을 보고 라우팅 정책을 정함
- **개발자 개개인** — 내 사용이 팀 평균 대비 어떤지, 어디서 새는지
- **재무/플랫폼** — 조직 전체 AI 지출 추세와 절감 근거

## 3. 어떻게 작동하나 (파이프라인)
```
① Ingest    팀원들의 AI 사용 로그 수집 (모델·작업유형·토큰·비용·사람·프로젝트)
② Detect    Token Cost Optimizer의 10패턴을 팀 로그에 적용 → 낭비 집계
③ Dashboard 팀 비용 추세 · 낭비 Top원인 · 사람/프로젝트별 분해 · 모델 믹스
④ Recommend Solar Pro 4가 팀 라우팅 정책 제안 ("이 작업유형 → 이 모델")
⑤ Approve   팀 리드가 정책 승인 (human-in-the-loop — 자동 강제 아님)
⑥ Track     정책 적용 전/후 비용 추적 → 실제 절감액 증명
```

## 4. 대시보드 화면 (패널)
- **한 줄 요약:** 이번 달 팀 AI 지출 + 절감 가능액 (예: "$4,200 지출, 추정 낭비 $2,900 = 69%")
- **낭비 Top 원인:** 10패턴 중 팀에서 가장 큰 것 (과잉 로딩 / 모델 오라우팅 / 캐시 깨짐 …)
- **사람·프로젝트별 분해:** 어디에 집중됐나 (비난이 아니라 코칭 대상 식별)
- **모델 믹스:** 최상위 vs 중가 vs 저가 사용 비율 → "기계적 작업의 X%가 최상위 모델"
- **라우팅 정책 카드:** 제안된 규칙 + 각 규칙의 예상 절감 + [승인/보류]
- **절감 추적:** 정책 적용 후 실제 비용 곡선 (전/후)

## 5. 예선 스킬의 조합 (composition story — 강력)
결선 서비스는 예선 스킬들이 **레고처럼 결합**됩니다:
- **Token Cost Optimizer** → 핵심 진단 엔진 (10패턴)
- **Ops Control Center** → 비용 급증 이상 감지 & 알림 (⑥의 alert 계층)
- **Data-Question Guardian** → 대시보드 숫자의 신뢰성 점검 ("이 절감 추정, 데이터가 뒷받침하나")

→ "예선에서 만든 스킬들이 결선 서비스의 부품이 된다"는 서사 = 심사에 강함.

## 6. Solar Pro 4 × 타임리 Agent 역할
- **Solar Pro 4:** 로그에서 낭비 패턴 분류, 라우팅 정책 추천, 자연어 요약("이번 주 낭비의 60%는 과잉 로딩")
- **타임리 Agent:** 로그 수집→진단→대시보드→정책→추적 워크플로 오케스트레이션, 스킬 호출 묶기

## 7. Human-in-the-loop (일관된 철학)
정책은 **제안**이고 실행은 팀 리드 **승인** 후. 자동으로 남의 모델 선택을 강제하지 않음 — 관찰 가능·투명·사람이 결정. (예선 6스킬 전체를 관통하는 원칙 그대로)

## 8. 성공 지표
- 팀 AI 비용 **절감률** (정책 적용 전/후)
- 라우팅 정책 **채택률** (제안 중 승인된 비율)
- "기계적 작업의 최상위 모델 사용 비율" 감소
- 데모: 샘플 팀 로그 투입 → 낭비 진단 + 정책 + 예상 절감 시연

## 9. 데모 MVP 범위 (결선 시연용)
1. 샘플 팀 로그(CSV/JSON) 업로드 — 3~5명, 여러 작업유형
2. 10패턴 진단 → 낭비 Top3 + 사람/모델 분해 시각화
3. Solar Pro 4가 라우팅 정책 3개 제안 + 예상 절감액
4. 팀 리드 승인 토글 → 절감 추적(시뮬레이션) 화면
> Demo Mode(시뮬 데이터)로 시작해 세팅 없이 시연 가능하게 — 예선 스킬들의 데모 철학 재사용.

## 10. 정직한 참고
- 실제 비용 추정은 **범위**로, 근거(어느 패턴에서 얼마)를 함께.
- 절감 후에도 산출물 **품질 검증**은 유지 — 싸게 하려다 틀리면 재작업이 더 비쌈.
- 타임리 실제 스킬/서비스 등록 스키마에 맞춰 세부 매핑 필요 (양식 확인 시 조정).
